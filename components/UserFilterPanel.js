export default class UserFilterPanel extends React.Component {
   
    static async getInitialProps (props) {
        this.setState(...props.state)
    }
    render (){
        return (

        )
    }
}